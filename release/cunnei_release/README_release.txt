发布包启动方式：
1. 推荐双击 `LaunchAvePoint.vbs`。
2. 启动器会自动判断 `savedUserCard` 是否为空：
   - 有卡密时：无控制台、无程序窗口启动。
   - 无卡密时：自动打开登录窗口，供手动输入卡密。
3. 不建议直接双击 `AetherRunner_vmp.exe`，否则不会走这个自动切换逻辑。

当前发布配置：
- 已关闭控制台窗口。
- 启动器会根据卡密状态动态切换是否显示 ImGui 程序窗口。
- 保留日志文件输出，日志默认写入 `logs` 目录。

脚本说明请查看 `AvePointGuide.txt`。
