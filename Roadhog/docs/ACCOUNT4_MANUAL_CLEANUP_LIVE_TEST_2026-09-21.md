# 账号 4 手动清包实机测试（2026-09-21）

账号 4 / 初次见面于 16:36:12 完成所有配置摆摊物品出售，并按用户要求停止在摆摊点。最终金币 24,615，背包、拍卖行及摊位窗口关闭，输入释放成功，没有回城或恢复挂机。金币和背包数量逐项核对一致。

本轮按用户保存的配置执行，期间根据用户补充修改出售优先级、限额处理、逐件出售和背包置顶频率，并从实际未完成阶段续跑；不是一次无中断测试。使用隔离构建和明确绑定账号 4 的实机测试入口，未覆盖桌面运行文件，也未提交或推送代码。

## 实际结果

| 阶段 | 结果 |
| --- | --- |
| 身份与回城 | 读取 `Desktop/script/4` 配置，确认角色初次见面、设备绑定及租约。起始金币 3,942,519；D9 回到贝达扎尔村，返程珠 27 → 26 |
| 丢弃 | 全部 28 项、31 个完成。一次悬停校验失败后保留任务并自动重试；用户要求改优先级时已丢弃 22 项，续跑完成其余 6 项 |
| NPC 出售 | 沿 9 点清包路径到巴内斯。先成交 9 件，收入 1,103,256；三件一批时下一批因售卖限额被拒绝，用户确认属于正常游戏行为 |
| 新出售逻辑复测 | 改为逐件出售、非武器优先、背包阶段开始时置顶一次后保持打开。又成功卖出幽冥之锁链腿甲 1 件，收入 61,120；下一件黄昏之盾未成交，保留剩余 10 件并正常返回 |
| 拍卖行 | 沿 10 点路径到草乌伦，先撤回全部 4 项旧挂售，再按配置上架，最后计算领取 2,043,440 金币；完成后逆走拍卖行路径 |
| 人工撤单与续接 | 第一段已确认上架 9 项。暂停修改期间，用户手动撤回其中 4 件武器并走回村口，明确要求继续按配置处理；续跑保留仍在售的 5 项，只上架背包中的 7 项，没有再次撤掉这 5 项 |
| 拍卖行最终状态 | 最终 12 项在售。累计确认上架 16 次，含人工撤回后重新上架的 4 件；总手续费 325,984。坚固的生皮子分 1,000 和 64 两叠，单价均 1,444 |
| 转移金币 | 沿 6 点摆摊路径到仓库号 HiApple，核对摊主对象 1711615144；首件坚固的生皮子单价 100,000，Shift + 右键填写 68，购买确认后转移 6,800,000，余款 24,351 |
| 摆摊 | 购买前冻结 22 项、264 个，单价均 1；三批为 10 / 10 / 2 项，收入分别为 10 / 252 / 2。每批同时确认清单耗尽、背包减少和金币增加 |
| 停止 | 16:36:12 最后一批售空，16:36:13 读到最终状态并释放输入，停在约 `(1411.248, 2034.391, 52.581)`；测试进程退出码 0 |

金币核对：

`3,942,519 + 1,164,376 - 325,984 + 2,043,440 - 6,800,000 + 264 = 24,615`。

背包核对包含全部丢弃、NPC 成交、初次撤回的 4 项、用户确认的 4 件人工撤单、16 次上架、68 个仓库购买、22 项摆摊及 1 个返程珠消耗；差异为零。最终背包占用 29 格。14 个魔石:盾牌防御+25 保留；旧挂售中未匹配当前交易配置的空间之长弓保留 1 件。购买的 68 个生皮子没有进入本轮摆摊。

## 修复与验证边界

- 魔石先排除于装备分类，避免名称含“盾”而被绿色装备出售规则误选；补充类型 3 武器，宝玉、枪按装备规则识别。
- 用户确认的新顺序为：拍卖行名单保留、显式黑名单丢弃、NPC 出售优先于摆摊，后续按实际剩余背包生成摆摊计划。白名单仍只阻止丢弃。
- 正式 NPC 出售每次提交一个背包条目，叠放保持原数量；非武器先处理，武器最后处理。同组按格子顺序，不修改用户价格或配置。
- NPC 只在阶段开始前准备背包一次，出售中保持打开。正式快照读取并校验背包边界，出售按钮未被背包遮住时才可点击。实机验证了腿甲成功出售和盾牌未成交，均保持背包打开。
- NPC 提交后仍等待成交证据；等待到期后，如果同一出售窗有效、列表已清空、物品实例/模板/数量全部未变且金币相同，记录 `bag_cleanup.sell.no_sale` 并继续后续流程。没有读取到限额数值，判断依据是可验证的未成交结果，不把读取失败或部分成交当成限额。
- 拍卖行上架阶段只置顶一次背包，后续每件核对最新坐标和悬停身份。最后 7 项在实机连续完成，期间未反复开关背包。内部显式续接入口要求已打开、无弹窗的登录页，仅处理剩余背包；正常完整入口仍先全部撤单。
- 新武器排序有模拟回归验证；后段实机已没有待 NPC 出售的武器，不能声称本轮完整实机从头验证了新排序。自动挂机恢复按本轮要求未执行。

各修改阶段均先补回归，旧实现分别复现魔石误选、摆摊抢占出售、限额中止和重复背包切换相关行为。完成隔离构建（最终 0 警告、0 错误），核对物品匹配、NPC 流程/解码、拍卖行、工作流及业务读取边界。NPC 回归覆盖单条目/整叠数量、武器最后、一次背包准备、限额拒绝、延迟到账、部分成交、列表未清空、读取异常、身份错误及取消；拍卖行回归覆盖连续 15 项只准备一次背包以及保留已完成挂售的续接。

证据目录：`.tmp/account4-manual-live-20260921/`。主要为 `initial-inspect.jsonl`、`run1.jsonl`、`run2-priority.jsonl`、`run3-sale-limit.jsonl`、`run4-auction-once.jsonl`、`run5-single-sale.jsonl` 和 `audit.json`。`run4` 在发送交易或移动输入之前发现人工移动/撤单并退出，经用户明确确认后才续跑。最终 `audit.json` 的 `goldEquationMatches`、`inventoryEquationMatches`、`soldOut` 均为 `true`，`failed=false`，`unexpectedInventoryChanges` 为空。
