# 账号 3 手动清包实机测试（2026-09-21）

账号 3 / KiraHa 按保存配置完成回城、丢弃、拍卖行、转移金币和三批摆摊。11:20:50 全部售空后停止在摆摊点，背包及摊位窗口关闭，输入释放成功；没有回城或启动挂机。最终金币 66,817，物品与金币差异核对一致。

这次从正式手动请求和 worker 开始执行，中途两处异常暂停后做了修复，从未完成阶段续跑；不是一次无中断通过。NPC 出售没有候选物品，按规则跳过。本轮使用隔离构建，没有覆盖桌面运行目录，也没有提交或推送代码。

## 流程与结果

| 阶段 | 实际结果 |
| --- | --- |
| 绑定与准备 | 读取 `Desktop/script/3` 的配置，核对角色 KiraHa、硬件映射及设备租约；开始时没有战斗 |
| 手动回城 | 10:56:16 按 D9，10:56:27 依据正式状态确认抵达贝达扎尔村；返程珠 18 → 17 |
| 丢弃与 NPC | 完成 9 项丢弃，重新匹配确认无剩余；NPC 出售候选为 0，未走出售路径 |
| 拍卖行 | 10 点路径前往草乌伦，核对 NPC、对话及委托交易；先撤下 2 件旧挂售，再按弹窗最低价上架 7 件，最后领取金币并逆行 10 点返回 |
| 拍卖行金额 | 上架手续费 23,160；领取已售金币 4,801,580；余额从 2,388,243 变为 7,166,663 |
| 仓库转移 | 走完 6 点摆摊路径，等待并核对 HiApple；选择键 NumPad8。首件生皮子单价 100,000，库存 440，购买 71 个，共 7,100,000，余额 66,663 |
| 摆摊 | 购买前冻结 23 项、154 个物品，配置单价均为 1，分为 10 / 10 / 3 项；三批收入分别为 138 / 13 / 3 |
| 停止 | 11:20:50 最后一批成交并关闭摊位，余额 66,817；停在约 `(1408.880, 2031.815, 52.581)`，角色存活 |

拍卖行上架包括 3 件空间之锁链肩甲、空间之锁链长靴、空间之长袍手套、罗刹之锤、罗刹之短刀。三件肩甲按各次弹窗实际显示的最低价登记，分别为 129,998、120,000、99,998。另一件撤回的空间之皮革护肩不匹配后续规则，保留在背包。

金额核对：`2,388,243 - 23,160 + 4,801,580 - 7,100,000 + 154 = 66,817`。

背包差异逐实例核对：9 项丢弃、6 件原背包拍卖物品及 23 项摆摊物品移除；生皮子 7 → 78，返程珠减少 1，撤回的空间之皮革护肩增加 1。撤回的另一件肩甲已重新上架。其余物品数量没有变化。

## 修复及验证

1. 首次 Shift + 右键未被游戏按组合键处理，整组 440 个进入菜篮子，尚未扣款。只读核对显示上方商品数量变为 0、菜篮子为 440；原适配器把正常的零库存行判成身份错误，导致持续复用先前正式快照，数量框等待超时。
   - 组合键按下及释放之间增加 100ms 的输入报告间隔，并在右键前重新核对对象、光标及取消状态；数量框和交易结果继续以正式快照验证。
   - 零库存商品保留身份校验，移除可点击坐标和悬停身份；菜篮子提供独立坐标及可空悬停编号，外来弹窗遮挡后不可操作。
   - 从正式快照确认菜篮子归属后，右键取消原待购买内容，核对余额不变，再成功输入 71 并购买。用户提出输入法可能影响 Shift；本轮调整了输入时序，但没有独立记录远端输入法状态，未单独证明输入法或时序是哪一个根因，也没有实现远端 Caps Lock 检测。

2. 第一批售出时，摊位先停止并清空列表，背包及金币尚未更新，原逻辑立即判成摆摊中断。
   - 保留全部成交证据要求，在窗口打开且清单已空的停止状态下，最多继续等待 8 秒核对物品减少和收入；不能仅凭空清单完成，缺少证据仍停止。
   - 重新核对第一批 10 项全部移除及 138 金币收入后，仅续跑剩余 13 项。第二批在 11:20:18.233 清单清空，11:20:18.611 收入到账；第三批对应时间为 11:20:50.260 / 11:20:50.579。两批均实际验证修复后正常衔接。

隔离构建通过，0 警告、0 错误。相关回归共 28 项通过：12 项原摆摊、12 项拍卖行，以及仓库购买、购买解码、配置摆摊、业务读取边界各 1 项。新增覆盖组合键延迟、取消时释放、按下 Shift 后对象变化、零库存整组菜篮子、外来弹窗遮挡、自动收摊后背包与金币分别延迟、清单为空但无成交证据。本轮开始前另通过正式 worker 插入、路径编排等专项检查；没有宣称运行完整测试套件。

证据目录：`.tmp/account3-manual-live-20260921/`。主要文件为 `initial-inspect.jsonl`、`run1.jsonl`、`purchase-diagnostic.jsonl`、`run2-resume.jsonl`、`stall-interruption-inspect.jsonl`、`run3-stall.jsonl`、`frozen-plan.json`、`audit.json`、`build-fix1.log`、`build-fix2.log` 及各 `*-regression.log`。测试结束的正式结果为 `soldOut=true`、`failed=false`，进程退出码 0。
