# 切频道过图状态调查（2026-09-14）

数据来源：`C:\Users\GoldGiven\Desktop\aion\aion202609091125.json`。以下 RVA 均相对当前 `Game.dll` 基址，已通过 `ChannelTransitionDecoder` 接入频道切换适配层。

| 字段 | 读取方式 | 已知含义与验证范围 |
| --- | --- | --- |
| 游戏主状态 | `Game.dll + 0xD6CA90`，uint32 | 地图加载入口将其设为 20，结束时恢复之前状态。账号 4 切频道实测 15 → 20 → 15。 |
| 地图加载阶段 | `Game.dll + 0xD65B10`，uint32 | 传送处理末尾置 0，资源与画面就绪后依次置 1、2。阶段 2 对应游戏的 Level ready 日志。切频道实测 2 → 1 → 2，未采到短暂的 0；过图初期也可能保持 2，不能单独作为结束判断。 |
| 本地角色 ID | `Game.dll + 0xD6CB08`，uint16 | 游戏加载完成回调会主动清零，正常游戏实测为 65535。读到有效的 0 与 DMA 读取失败必须分开；不能单独用它判断 VMM 断开。 |

`0xD65B10` 也可表示为游戏对象地址 `Game.dll + 0xD647C0` 加 4944（0x1350）。这个游戏对象内嵌在模块全局区，不能把其首字段（虚表指针）当成游戏对象地址再解引用。初次临时采样的 phase 使用了错误解引用，已更正；仅采用 `baseline-corrected.log` 和 `switch-corrected.log` 的 phase 数据。

静态证据：

- `sub_1800122D0`：保存主状态，通过 `sub_180340E80(..., 20)` 进入地图加载，发出 StartLevel / StartIndun。
- `sub_180340E80`：将传入状态写入 `dword_180D6CA90`。
- `sub_180378D20`：加载 `Textures/Loading/loading_%s.dds` 等过图画面资源。
- `sub_180342640`：清零本地角色 ID；主状态为 20 时弹出并恢复原状态。
- `sub_1800489E0`：调用地图加载入口，末尾将 `Game.dll + 0xD65B10` 置 0。
- `sub_180024E80`：游戏对象 +4944 的阶段从 0 到 1，再到 2；就绪后打印 Level ready。

账号 4 实测：主状态 15、阶段 2、本地角色 ID 65535、地图 220050000、频道 2。19:14:03 提交一次移动至频道 3，之后未观察到主状态变化或频道变更，不能据此确认成功过图，也不能仅凭未变更确认是冷却原因。采样与点击记录保存在 `.tmp/channel-loading-probe/`。

19:16:37 再次提交移动，19:16:47 确认从频道 2 切到频道 3。同一 VMM 连接持续采样期间没有读取异常，没有重连，地图 ID 始终为 220050000。记录如下（相对于采样开始，约 50 毫秒一次，时间不是游戏精确帧时间）：

| 毫秒 | 主状态 | 加载阶段 | 角色 ID | 频道 |
| --- | --- | --- | --- | --- |
| 0 | 15 | 2 | 65535 | 2 |
| 4265 | 20 | 2 | 0 | 3 |
| 6277 | 15 | 2 | 0 | 3 |
| 6340 | 15 | 1 | 0 | 3 |
| 7351 | 15 | 2 | 0 | 3 |
| 7671 | 15 | 2 | 65535 | 3 |

本次角色 ID 为 0 的采样窗口约 3.406 秒。频道编号在过图开始时已变为 3，主状态恢复 15 和加载阶段恢复 2 也早于角色恢复，因此仅依据频道编号、主状态或加载阶段中的任一字段都不足以安全恢复挂机。没有同步采集位置/移动状态，无法由此次记录证明此前切换失败是移动限制导致。

已修复的问题：`TryReadLocalPlayer` 原先把本地角色 ID 的读取失败和成功读到 0 合为同一个错误。现在有效的 0 表示角色暂不在场景中，不累计重连失败；本地 ID 读取失败但明确读到主状态 20 时同样等待过图。真正的读取失败仍保留原重连处理。

接入方式：新增封闭注册的 `channel_transition` Stable 快照，业务只读取正式快照。点击后每 200 毫秒观察，5 秒内没有观察到过图且角色仍在场景时返回挂机，按原一分钟规则重试。观察到过图后持续等待；主状态 15、阶段 2、本地 ID 非零，并在同次直接读取中拿到对应角色及频道、前后场景和频道检查一致，才发布恢复。不会用独立 player 通道的旧缓存拼出恢复快照。过图时暂不读取战斗对象或执行输入；超过 60 秒只告警，继续等待，停止按钮可取消。已取消固定 10 秒延迟。

启动时匹配目标或恢复后验证成功，会设置本次 worker 的完成标记并停止频道读取。修改目标或重新启动清除标记；完成后手动切换频道不会被自动纠正。不固定不读取频道。未完成时频道检查最多每分钟一次，脱战活动仍按原节奏观察。

新流程实测（19:49，账号 4）：19:49:25.495 点击移动，19:49:28.079 记录 `channel_switch.loading`，19:49:31.768 记录 `channel_switch.world_ready`，实际频道 3、角色 ID 65535，返回成功。从点击到角色恢复确认约 6.27 秒，没有固定等待满 10 秒。底层采样同样记录到主状态 20、角色 ID 清零、主状态和阶段先恢复、最后角色 ID 恢复；采样无 `POLL_ERROR`。证据在 `.tmp/channel-loading-probe/new-transition-switch.log`。这是共用切换流程的实机验证，挂机任务恢复和成功后停止读取由调度回归测试覆盖。
