namespace SampleProject.Application;

public static class ProjectModules
{
    // Add typed module factories here if useful. Register them in Bootstrap/ProjectComposition.RegisterModules.
    // Each registration creates new module instances for its account session.
}
